const Notes = () => {
    return(
        <>
        <h1>Notes on react</h1>
        <ul>
            <li>Whenever Client Request on web server it gets Html, CSS and JavaScript in response. After that JavaScript take over whole application</li>
            <li>In SinglePageApplication Loads only single page of Html and dynamically update  content as the user interacts with thr app.</li>
        </ul>
        </>
    )
}
export default Notes;